# P5 · 报告（方法论指令）

入口：仅经 P4 门进入（gate-exit:P4 在场）。首节强制披露 degraded 未闭合格（budget-exhausted 终态的披露清单完备性由 terminal-gate 断言执法）。

## duty（命令序列）
1. tanyin-report 聚合器从 13 表确定性重建正文（模板+账本数据，零 LLM 方差；LLM 仅写执行摘要与修复建议叙述段，须引用 finding ID）。
2. report-draft.md 附「范围外观察」附录：out_of_scope facts 列示供客户扩授权决策（接设计 §5.4 边界生长闭环→P3 scope-amended）。
3. 交付前脱敏终检：redact-scan --target report/（占位符零泄漏，任一真值残留=阻断导出）。

## entry 检查单
- gate-exit:P4 在场（verify-chain 跳门检测兜底）
- degraded 披露清单齐备（P3 budget-exhausted 终态时——未闭合格逐项列示）

## exit 断言
- tanyin-phases gate --goal-dir <D> --phase P5 --timestamp <T>，断言三条：
  1. terminal-gate（矩阵无空格 | degraded 披露清单完备）
  2. tanyin-report --lint（schema lint+脱敏检查 PASS）——批次 6 交付前该断言=ENV-HALT（引擎退出码 2，可重跑，非门禁失败；PROTOCOL.md §1 判定表末行）
  3. redact-scan --target report/（占位符零泄漏）
  on_fail=halt。

## 回边
- 本门无事件回边；范围外观察引出的扩授权走 P3 scope-amended（不回本门改稿——报告版本随账本前进重生成）。
