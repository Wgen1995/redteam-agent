# P5.5 · 签发门（方法论指令）

入口：仅经 P5 门进入（gate-exit:P5 在场）。未签发报告禁止导出（cli 层：导出命令校验签发行）。

## duty（命令序列）
1. 人审：报告全文+degraded 披露+范围外观察附录交客户签署方审阅（人在回路，L3 分级审批纪律——展示原始证据非 LLM 摘述）。
2. approve（command_hash 绑定聚合产物文件哈希——签署决定不可抵赖）。
3. report-signed.md 定稿（签署版与 draft 并存，签署行入账）。

## entry 检查单
- gate-exit:P5 在场（verify-chain 跳门检测兜底）
- report-draft.md 在位且 redact-scan 已过

## exit 断言
- tanyin-phases gate --goal-dir <D> --phase P5.5 --timestamp <T>，断言一条：
  1. approve --verify-signoff（approvals 存在对应 approved 行）

## 回边
- 本门无事件回边；人审驳回=原地改稿重审（报告随账本前进重生成后再签）。
